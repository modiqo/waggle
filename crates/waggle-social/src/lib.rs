//! # waggle-social — the human face of the same token (stub, CP-8)
//!
//! Pure renderers from `(manifest, host config)` to channel artifacts:
//! post text, Slack blocks, email, OG meta built exclusively from mint-time
//! snapshot metadata (invariant I-3 — the system never scrapes targets),
//! and QR codes for the `qr-event` channel (design doc `05`).
//!
//! Social minting is a capability of the primitive, not a battlefront
//! (design doc `01 §3`): the same token that hands a subagent its variant
//! unfurls truthfully in Slack.
